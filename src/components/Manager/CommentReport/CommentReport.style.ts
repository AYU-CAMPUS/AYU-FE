import styled from "@emotion/styled";
import {
  AcceptanceBtn,
  RefuseBtn,
  RequestListTable,
} from "../RequestList/RequestList.style";

export const CommentReportTable = styled(RequestListTable)``;

export const ApplyBtn = styled(AcceptanceBtn)``;
export const CancelBtn = styled(RefuseBtn)``;
